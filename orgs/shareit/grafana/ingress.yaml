apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grafana
  namespace: tools
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
    # traefik.ingress.kubernetes.io/router.entrypoints: websecure  # Descomente para HTTPS
    # cert-manager.io/cluster-issuer: "letsencrypt-prod"  # Descomente se usar cert-manager
spec:
  ingressClassName: traefik
  rules:
    - host: grafana.shareit.com  # Altere para seu domínio
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: grafana
                port:
                  number: 80
  # tls:  # Descomente se quiser HTTPS
  #   - secretName: grafana-tls
  #     hosts:
  #       - grafana.shareit.com
